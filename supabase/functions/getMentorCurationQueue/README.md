# getMentorCurationQueue (dry-run)

Returns a static queue for mentors to curate.

- Method: GET
- Response: { mode: 'dry-run', items: { course_id, title, confidence, marketAlignment, skillGapCoverage }[] }
